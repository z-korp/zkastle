{
  "version": 2,
  "builds": [
    {
      "src": "vite.config.js",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    },
    {
      "src": "api/**/Cargo.toml",
      "use": "@vercel/rust"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/vrf/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
